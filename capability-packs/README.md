# Capability packs

This directory will contain reviewed, versioned capability manifests. A pack can expose only typed
arguments and a fixed execution template. It must never expose arbitrary console text, shell input,
scripts, reflection, or unrestricted files.

Manifest loading and execution are Phase 9 work. The Phase 0/1 repository contains no executable
capability pack.
