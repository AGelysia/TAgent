# Safe read-only example

`server-version.example.json` demonstrates the smallest closed, read-only manifest. Its `example`
status makes it unconditionally non-executable; it is schema documentation, not an enabled pack.
