# Homes example pack

`set-home.draft.json` is review material for typed argument and policy handling. Its `draft` status
makes it unconditionally non-executable; it is not an approval record or an installed capability.
