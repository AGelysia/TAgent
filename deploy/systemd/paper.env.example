# Install as /etc/tagent/paper.env, owned by root with mode 0600.
# Paper receives only the shared Runtime token and the preview opt-in.
MINECRAFT_AGENT_SERVER_TOKEN=replace-with-the-same-new-64-hex-token
MINECRAFT_AGENT_BUILD_PREVIEW_ENABLED=false
